import { PuzzleConfiguration } from './puzzle-configuration';

describe('PuzzleConfiguration', () => {
  it('should create an instance', () => {
    expect(new PuzzleConfiguration()).toBeTruthy();
  });
});
